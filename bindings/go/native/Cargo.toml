[package]
name = "typikon-go-native"
version = "0.3.1"
edition = "2024"
publish = false

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
typikon = { path = "../../.." }

[build-dependencies]
typikon = { path = "../../.." }
