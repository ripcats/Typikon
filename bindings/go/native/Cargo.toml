[package]
name = "typikon-go-native"
version = "0.2.0"
edition = "2024"
publish = false

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
typikon = { path = "../../.." }

[build-dependencies]
typikon = { path = "../../.." }
